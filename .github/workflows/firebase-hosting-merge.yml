name: Deploy to Firebase Hosting on Merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter (to match Dreamflow version)
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.38.5'
          cache: true

      - name: Verify Flutter Version
        run: flutter --version

      - name: Install dependencies
        run: |
          flutter pub get
          flutter clean

      # Using the most compatible flag for the 3.38.x branch
      - name: Build Web
        run: flutter build web --release --web-renderer canvaskit

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MOONLITELY_ENTERTAINMEN_XT654Z }}'
          channelId: live
          projectId: moonlitely-entertainmen-xt654z